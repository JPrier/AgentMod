[package]
name = "agentmod-acp"
description = "Agent Client Protocol frontend for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "agentmod-acp"
path = "src/main.rs"

[dependencies]
agentmod-acp-data = { version = "0.1.0", path = "../data" }
agentmod-acp-dependency = { version = "0.1.0", path = "../dependency" }
agentmod-acp-logic = { version = "0.1.0", path = "../logic" }
agentmod-acp-service = { version = "0.1.0", path = "../service" }
agentmod-protocol-support = { version = "0.1.0", path = "../../../core/protocol-support" }
serde_json.workspace = true
tokio.workspace = true

[lints]
workspace = true

[package.metadata.agentmod]
kind = "composition-root"
process = "acp"
