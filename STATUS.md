# AgentMod Status

Last updated: 2026-07-31

AgentMod begins from an empty upstream repository. This file reports only verified
implementation state; planned interfaces and scaffolds do not count as implemented.

## Current phase

Generic node-execution transition, Phases 2–5 — live exact-executor dispatch,
canonical variables, parallel recovery, and plugin-node execution. The
style registry, immutable session binding, generic arbitrary-graph executor,
versioned declarative, Ephemeral, Persistent, and Research v1.2 generic paths,
and style-selected context composition are live. Exact Declarative, Ephemeral,
Persistent, and Research v1.1 histories plus the planner-worker built-in-shaped
graph retain versioned compatibility adapters. Research v1.2 and
Planner-worker v1.3 have passed exact-plan, restart, and pure-replay process
E2Es on Windows and Ubuntu/WSL2. The current v1.4 process matrix additionally proves
two genuinely overlapping child turns in runtime-owned branch workspaces,
child-owned edit/test/diff artifacts, deterministic integration, structured
review rejection and revision, and unchanged parent workspace content. Exact
Planner-worker v1.1 remains on its frozen compatibility adapter.
No/file/SQLite memory and none/sliding-window/tool-output-eviction compaction
run through canonical lifecycle boundaries before provider requests. Typed
summary compaction now also executes live in runtime logic: it creates a
deterministic bounded typed projection from canonical entries, retains required
records, reaches the provider without changing canonical history, and survives
pure replay. Dedicated Windows and Ubuntu/WSL2 process runs prove exact
style/execution-plan retention, daemon replacement, typed provider projection,
unchanged canonical conversation history, and pure replay. Completed context
phases recover exactly; an open external context phase still fails closed rather
than redispatching. Turn-completion automatic memory writes are also live. The
runtime derives bounded typed content after a successful turn, binds the exact
provider/model/options/input request, and commits
`memory.write_proposed` → `approved` → `dispatched` → `completed` per configured
scope. Policy runs before dispatch, mandatory policy is revalidated before
restart access, proposal-only recovery fails closed, and dispatched file writes
reconcile only through deterministic idempotent identity. Windows and
Ubuntu/WSL2 process kill-cuts prove recovery after the file record is durable but
before terminal commitment, with both harness paths unavailable, no provider
redispatch, one retained record, and byte-stable pure replay. Exact
per-iteration writes are now live in runtime logic: every successful loop
transition retains its destination, counters, canonical sequence, and event
checksum, and each boundary owns a distinct versioned write identity plus
bounded conversation, node-output, and artifact evidence. Three-iteration
Research tests prove exact replay, dispatched-write reconciliation, and no
provider redispatch. Dedicated Windows and Ubuntu/WSL2 process tests kill the
runtime after the first of three iteration writes is physically durable,
restart with both harness paths unavailable, recover all three exact writes,
and prove byte-stable pure replay. Dedicated Windows and Ubuntu/WSL2
session-completion process tests kill after the terminal memory record is
durable but before completion, restart without either harness, recover the
same exact write, and prove byte-stable pure replay. Native automatic-memory
`ask` now retains a distinct exact continuation and approval subject. Windows
and Ubuntu/WSL2 process tests prove restart while pending, one approved write,
effect-free duplicate approval, and terminal denial without dispatch; plugin
continuation serialization and identities remain unchanged. New automatic content excludes
tool, artifact, runtime, and child-handoff entries and remains session-scoped.
A bounded runtime-owned classifier now attaches an explicit class to every
retained entry; high-confidence credentials, keys, signed tokens, paths, URLs,
process/pipe handles, control characters, and over-bound values reject the
whole automatic projection before proposal. Portable runtime references use an
exact allowlist. Windows and Ubuntu/WSL2 session-completion process tests cover
the retained classification envelope and raw secret/path/URL/handle rejection.
Broader semantic DLP remains incomplete.
Artifact-handoff compaction is now live and recoverable. It writes a canonical
`agentmod.context-artifact.v1` document containing the complete typed provider
projection and provenance through the existing policy-gated artifact
logic → data → dependency path. The session retains exact artifact
proposed/approved/dispatched/completed events plus a hash-only replacement
approval; the projection and replacement envelope bind the same
content-addressed object. Approved and dispatched cuts resume or reconcile
without a second write, ambiguous policy cuts fail closed, pure replay remains
storage-free, and live provider resumption re-hashes and validates the retained
object. Dedicated Windows and Ubuntu/WSL2 runtime/harness/CLI process runs prove
the stored document, daemon replacement, unchanged history, immutable plan, and
pure replay. A separate cross-platform finalize cut kills after the
content-addressed object and metadata are durable but before dependency return;
restart deduplicates the same bytes, commits one canonical lifecycle, performs
one provider request, and leaves journal and artifact bytes unchanged on replay.
Research loops run
bounded fresh-context iterations with immutable findings, and the compiled
declarative fixture executes branch, approval, native-tool, loop, and terminal
nodes. Planner-worker-reviewer now runs its compiled plan, spawn, wait, join,
integrate, review, reject, revision, and terminal graph through runtime-managed
child sessions with canonical replay state. Plugin-sourced styles now activate
style-selected process interceptors and asynchronous committed-event observers;
activation and blocking invocation state is canonical and replay-inspectable.
Harness selection is now an exact per-session binding: the runtime registry
advertises native and deterministic fixture adapters, validates style-required
capabilities before creation, routes model work by the retained harness ID, and
fails closed after restart if adapter identity or capabilities change.
Session inspection now adds a stable replay-derived orchestration projection,
and the TUI Graph view renders its active/control node, graph progress, known
next transitions, remaining canonical budgets, pipeline, memory/compaction,
child/join/reviewer state, and termination. The TUI now also performs atomic
branch-with-style selection through its normal four-layer protocol path and
switches directly to the deliberately restyled child. CLI and TUI session
creation now select runtime-advertised memory and compaction components plus
SDK-validated hard execution budgets; the
SDK normalizes and recompiles each override into a distinct immutable binding
whose exact hashes survive restart validation.
The runtime node-executor capability registry now compiles an exact immutable
per-node execution plan before session, branch, or child-session persistence.
The retained plan binds executor ID and version, source and process boundary,
runtime API requirement, required and resolved capabilities, compiled-node
configuration identity, the complete registry hash, and a canonical plan hash.
Normal style compatibility checks and turn preflight revalidate the exact
retained implementations and fail closed on registry or implementation drift
without selecting a replacement. Canonical style-execution initialization
retains the plan identities, resolutions, initial node, variables, budgets, and
run identity for pure replay. Structurally valid graphs whose nodes all resolve
are no longer rejected merely because they lack a built-in adapter profile.
Execution-plan compiler generation `agentmod-runtime-node-plan@3` always selects
generic dispatch from the exact persisted resolutions. It never consults style
identity, node labels, graph topology, variable shape, or adapter kind as a
fallback; if any retained resolution has no supported exact generic handler,
turn preflight fails closed with the stable
`UnsupportedGenericExecutionPlan` error. Only generation `@2` historical
bindings may select a frozen compatibility adapter.
Runtime logic now owns a bounded typed node command/input/state/budget/outcome
contract and one centralized implementation router keyed by the exact persisted
executor ID, version, source, boundary, and node kind. Compiled cursors carry
and validate that exact resolution. Arbitrary non-profile graphs composed of
context construction, model requests, tool gates, durable user approval,
artifact persistence, conditional, bounded-loop, turn/session completion, and
structured-failure nodes execute and replay through the generic router without
adapter classification. The effect nodes cross the existing provider, tool,
continuation, and artifact use cases; a non-built-in test graph survives a
pending approval restart and then completes its tool and artifact effects
exactly once. Declarative v1.2 uses exact executor dispatch under persisted plan
compiler generation `agentmod-runtime-node-plan@3`; exact Declarative,
Ephemeral, Persistent, and Research v1.1 plus generation `@2` histories retain
legacy adapter recovery. Planner-worker v1.4 now runs through the same generic
executor path; only exact v1.1 retains the compatibility adapter.
Constrained `emit_event` nodes now execute
through the same exact-ID router and commit a static canonical envelope with a
runtime-owned identity; replay deduplicates by immutable run/branch/node/attempt/
loop/step identity. Exact `runtime.delay@1.0.0` and
`runtime.schedule@1.0.0` nodes use durable scheduler-bound continuations and
canonical schedule lifecycle records. Exact `runtime.child-message@1.0.0` nodes now use the generic
runner to commit a policy-approved parent outbox, seal a typed canonical child
event, and reconcile the exact child-journal receipt without projecting a fake
user message. Canonical parallel/join state and a production turn seam are
implemented and restart-tested for pure and effectful branches. Exact branch
tool approvals retain the immutable work/executor/plan identity, and tool,
schedule, and child denials return through branch failure policy without
redispatch. Normal user-manifest admission now persists and restart-revalidates
the exact parallel/join plan through the native registry. The exact
`runtime.parallel@1.0.0` and `runtime.join@1.0.0` entries are available after
normal admitted `run_turn` tests proved pure completion plus approval/artifact
waiting, restart, typed output, and no duplicate effects. Admission still
rejects unsupported nested executor classes and malformed or nested regions
with stable structural diagnostics. Plugin-node lifecycle,
receipt, cancellation, and production data adapters are implemented and unit
tested. A Windows process E2E now proves direct isolated invocation, the
production coordinator, and a renamed arbitrary graph through live generic
`TurnLogic`, including approval restart, duplicate resolution, durable
receipts, and no redispatch. Canonical rejection of invalid completed proposals
is live. Validated nonempty plugin state now crosses a runtime-logic-owned
application port only after proposal validation, variables, and budgets; its
hash-only receipt is committed before node completion. Definite or ambiguous
state-store failure becomes a canonical terminal record and is never silently
retried. Hash-only Invocation/Session state receipts and replay-derived
generation chaining are implemented through the plugin-host boundary; Turn
application is covered by the Windows process suite. Protocol v4 also provides an
authorization-, generation-, and hash-bound Session-state read through the same
plugin-host store; a Windows host-restart process test proves exact replay and
substitution rejection. Unit tests cover exact prior-state injection and reject
identity, generation, hash, and scope substitution. Declared plugin-proposed
filesystem and network actions traverse the ordinary runtime policy,
continuation, receipt, and recovery paths; additional runtime-action classes
remain incomplete.
Immutable plugin context transforms are now live for the
`before_model_request` lifecycle. A style retains each exact plugin, transform,
version, declaration hash, lifecycle, and configuration reference in compiled
order. Invocation follows runtime logic → runtime data → runtime dependency →
plugin-host service → logic → data → dependency → isolated plugin process.
Canonical proposed/dispatched/completed/failed/ambiguous,
replacement-approval, and projection-replacement events retain the exact
identity. A sealed terminal receipt is persisted before the terminal event;
restart reconstructs from that receipt without redispatch, while a dispatched
call without a receipt is ambiguous and fails closed. Runtime logic validates
the live declaration with a closed, bounded schema subset covering nested
objects and arrays, required fields, enums, primitive types, length/item/numeric
bounds, exact large-integer comparison, and strict unknown-keyword rejection.
It converts invalid terminal output into a durable failure, revalidates
mandatory context preservation and policy before applying a replacement, and
never grants plugins envelope or state-mutation authority. The isolated plugin
process suite passes all nine
cases on Windows and Ubuntu/WSL2, including live coordinator invocation,
receipt-only restart, declaration loss, invalid result, timeout, and ambiguous
execution.
Plugin-provided memory retrieval and compaction now use the same exact
runtime-owned operation lifecycle. The immutable binding selects the exact
plugin/component versions, declaration hash, configuration reference, handler,
timeout, schemas, and bounds. Runtime logic proposes and authorizes the exact
request, commits dispatch before plugin-host entry, validates the typed result,
persists a sealed terminal receipt, separately authorizes application, and
commits only a bounded canonical projection. Recovery reduces an exact receipt
without loading the worker or redispatching it, but live plugin composition must
revalidate before a later effect. Missing, corrupt, substituted, invalid, and
timed-out outcomes fail closed. Real Windows and Ubuntu/WSL2 process suites
prove turn-start, context-node, and before-model memory retrieval, plugin
compaction, invalid/timeout rejection, and duplicate suppression. The Windows
suite additionally proves the receipt crash cut, offline receipt reduction,
live revalidation before the next effect, and unavailability before a new
operation.
The graph compiler now retains bounded typed configuration for child messaging,
joins, parallel branches, delays, schedules, user-space event emission, and
plugin-backed executors. It also compiles canonical variable declarations with
type, scope, producer/consumer, mutability, merge, size, and classification
contracts; rejects undeclared reads/writes and unsafe parallel conflicts; and
canonicalizes configuration values deterministically. Legacy manifests remain
valid until they opt into canonical variables. A pure runtime-logic variable
kernel now validates and hashes typed values, access, versioned writes, branch
write safety, deterministic merges, replayed serialization, and exact condition
eligibility. Its isolated event/reducer contract prepares and independently
revalidates declaration, assignment, merge, scoped removal, and failed-validation
records for pure replay. Those records now have stable `RuntimeCommittedEvent`
types and reduce into a serde-defaulted style-execution projection. Style
initialization derives declarations and typed initial values from the compiled
graph and immutable legacy JSON without live queries. Generic control-node
reads, assignments, deterministic transitions, and restart recovery are live
in `TurnLogic`. Root context, model, tool, approval, artifact, child-message,
event, delay, and schedule nodes commit typed outputs only after canonical
terminal evidence. Delay/schedule timestamps and durations come from the
persisted resolution record, and legacy in-flight executions fail closed when
those values are required. Parallel branch output projection now uses fresh
canonical branch inputs for pure and effect nodes. Shared-variable
contributions and deterministic join merges are canonical and reconstruct
identically across every tested restart prefix. Real Windows and Linux daemon
E2Es now admit an arbitrary 14-node user graph, persist its exact plan, restart
during parallel approval and durable delay, run
model/tool/artifact/loop/event/join nodes, reconstruct typed variables,
suppress duplicate approval/effects, and complete through pure replay on the
default worker stack. Cross-platform
scheduler storage now uses a portable encoded filename while retaining the
exact raw graph schedule ID.
Native and plugin automatic-memory approval, concurrent planner child turns,
runtime-owned branch workspaces, and child-owned result/diff/test evidence are
live and cross-platform process-tested. Broader semantic secret detection,
other planner workspace modes, and the complete write-denial matrix remain
incomplete.

## Completed capabilities

| Capability | Status | Evidence |
|---|---|---|
| Repository orientation | Integration tested | Empty upstream clone confirmed; Rust 1.91.1 Windows MSVC toolchain recorded |
| Product/acceptance plan | Implemented and unit tested | `.omx/plans/prd-agentmod.md` |
| Verification contract | Implemented and unit tested | `.omx/plans/test-spec-agentmod.md` |
| Initial architecture/process/dependency maps | Implemented and unit tested | `docs/architecture/initial-maps.md` |
| Cargo architecture enforcement | Integration tested | `cargo run -p xtask -- architecture --manifest-path Cargo.toml`: 89 packages, no violations; intentional negative fixture tests pass |
| Versioned protocol framing/negotiation | Implemented and unit tested | Bounded CBOR frame and capability/version tests in `agentmod-protocol-support` |
| Runtime N-tier health slice | Integration tested | Real dependency → data → logic → service → composition-root path; layer tests and runnable binary |
| Harness N-tier health/capability slice | Integration tested | Separate harness binary and four layer test suites |
| Headless CLI daemon/session slice | End-to-end validated | Four-layer CLI uses authenticated local RPC for human/batch JSON plus live NDJSON turns, durable session create/list, point-in-time inspect/replay, and atomic branch; Windows named-pipe process E2Es pass and Unix automation is present |
| Terminal frontend vertical slice | End-to-end validated | Independent Ratatui `service → logic → data → dependency` crates use only the runtime protocol; authenticated bootstrap, lazy session listing, canonical-history paging, bounded continuous cursor subscription, multiline UTF-8 editor/history, session switching, Chat/Events/Context/Graph/Styles/Harnesses/Schedules/Plugins/MCP/Runtime/Help views, live style details/diagnostics, committed streams with credit acknowledgements, cancellation, permission modal, token accounting, atomic `/branch <sequence> [style]`, runtime-advertised `/memory` and `/compaction` selectors, SDK-validated `/budget` selection, exact plugin lifecycle management, durable schedule management, runtime-owned MCP OAuth begin/status/cancel, bounded read-only canonical artifact/child/process resource inspection, and workspace-confined rich attachments are implemented. Attachment loading rejects traversal, external paths, links, devices, secret-like or unsupported content, duplicates, and size/count overflow; accepted image/blob content uses the ACP-compatible versioned rich envelope and transient bytes clear after submission or session change. Focused TUI tests and warnings-denied Clippy pass. Windows and Ubuntu/WSL2 process runs prove OAuth management, canonical resource projections, approval/delay/child/process restart cuts, and rich image/blob submission with restart, replay, containment rejection, and transient-state clearing. LSP management remains unavailable because the runtime exposes no stable canonical LSP-management projection. |
| ACP frontend vertical slice | End-to-end validated | Independent `service → logic → data → dependency` crates use the official `agent-client-protocol` 2.0 SDK and stable ACP wire version 1. Capacity-one layer-owned streams preserve runtime credit-window backpressure and emit each `session/update` without turn buffering; dropped client streams issue runtime cancellation, and active cancellation is registered before the endpoint spawns response work. The stdio adapter negotiates capabilities, creates and loads runtime sessions, accepts bounded rich content, and maps durable approval, denial, and cancellation outcomes without duplicate execution. ACP MCP declarations cross layer-owned request types into an immutable sanitized session binding with an exact declaration hash and encrypted, authenticated activation bootstrap. Windows and WSL/Linux process tests prove rich-content bounds, stdio/HTTP/SSE activation and invocation, cursor resumption, cancellation, exact-load rejection, and recursive secret non-disclosure. Branch and immediate-child matrices additionally prove exact sanitized binding inheritance, authenticated source bootstrap copying, child session-ID rewrite with fresh nonce/AAD/ciphertext, invocation across runtime restart and exact recovery, fail-closed declaration/parent-envelope/child-envelope tamper, and effect-free replay. Immediate child inheritance requires explicit style-wide `inherit_mcp` and the `mcp` tool gate; default/false is empty. Transitive/grandchild inheritance and macOS execution are not established. |
| Event envelope and integrity | Implemented and unit tested | Typed envelopes, classifications, BLAKE3 integrity and tamper tests |
| Blocking event pipeline | Implemented and unit tested | Deterministic compile/order, decisions, timeouts, failure policies and bounded observers |
| Continuation resume-once core | Implemented and unit tested | Concurrent resume, cancel and expiry tests |
| JSONL journal dependency | Integration tested | Real filesystem append/scan, checksum chain, sequence/duplicate rejection, concurrent append, trailing recovery and interior quarantine tests |
| Canonical conversation projection | Implemented and unit tested | Structured entries and replacement provenance preserve canonical history without fake user messages |
| Pure session replay reducer | Implemented and unit tested | Integrity, sequence, approval-once, lifecycle, replay-to-sequence and context-replacement tests |
| Durable replay and branching | End-to-end validated | Runtime inspect/replay endpoints reduce verified journal prefixes without dispatching effects. Small branches remap exact structured history; histories over 32 entries or 64 KiB atomically store a hash-bound private/session-retained context artifact and a maximum 16-entry/64-KiB live projection with explicit ancestry/provenance. `runtime_replay_branch.ps1` and `runtime_large_branch_artifact.ps1` prove exact small branches, bounded large branches, independent continuation, and unchanged parents; Unix automation is present |
| Permission precedence kernel | Implemented and unit tested | Deterministic rules and mandatory-policy-last deny precedence |
| Content-addressed artifact storage | Integration tested | Transactional chunked writes, BLAKE3 verification, deduplication, bounded ranges and cleanup tests |
| Validated immutable snapshots | Integration tested | Atomic writes, journal anchors, reducer/schema compatibility, corruption isolation and latest-valid selection tests |
| Expression and graph kernels | Implemented and unit tested | Constrained expression evaluation plus bounded graph parsing, validation, and deterministic compilation. The compiler retains typed native/plugin node configuration and canonical variable declarations, enforces configuration-kind/size/timestamp/plugin/event contracts, producer/consumer/read/write/type/scope rules, merge-policy/type compatibility, explicit secret-reference typing, and parallel merge/serialization policy, while preserving legacy manifests that have not opted into canonical variables. Parallel fan-out now requires unconditional uniquely labelled members that exactly match the configured join set; compilation rejects duplicate targets, policy/threshold mismatch, overlapping or cross-entered branch regions, outside entries, and completion paths that bypass the join. Native effect-output declarations are now checked for exact slot compatibility, duplicate consumers, nested runtime-owned values, and ambiguous singular/plural child results; no-write legacy graphs remain outside this gate. Runtime-owned variable assignment/merge/removal events are live in generic root and parallel `TurnLogic` execution and restart recovery. |
| Canonical typed variable kernel and session replay | Live in generic root Turn execution and restart-tested parallel projection | The bounded typed environment retains declaration, assignment, merge, removal, and validation-failure events with exact work, version, value-hash, scope, merge, size, and classification identity. `TurnLogic` initializes declarations and values before entry, reads replayed values for exact node inputs, commits declared outputs only after terminal executor evidence, and evaluates transitions from freshly replayed state. Root context, model, tool, approval, artifact, child-message, event, delay, and schedule paths use the runtime-owned projector. Parallel pure and effect nodes receive fresh branch inputs; shared writes become canonical contributions and merge in stable configured order at join. Focused reducer, coordinator, projection, Turn, and parallel restart tests reject undeclared, stale-version, unsafe-shared, missing, extra, ambiguous, mistyped, secret/handle, and oversized writes. An explicit process-level sequential pure-write-to-effect-read restart case remains pending. |
| Canonical variable operation coordinator | Pure planner and live generic-control integration tested | The journal-agnostic coordinator consumes replay, declarations, and exact `NodeWorkIdentity`; derives writers; validates declared reads/writes, ownership, branch policy, type, classification, size, and version CAS; and returns work-bound canonical payloads. Initialization emits missing declarations in graph order and initial assignments in canonical name order. The strict output planner projects the executor result to the node's exact write set, requires all non-optional writes, performs canonical type conversion, and reserves timestamp/duration assignment for explicit runtime-recorded inputs. Recovery now treats an exact pending seeded assignment audit as recoverable rather than a false version conflict. Focused tests cover initialization prefixes, legacy serde, tampering, typed output, missing/extra/optional writes, artifacts, secrets, runtime values, versioning, read-modify-write inputs, and restart-identical transitions. |
| Canonical variable journal coordinator | Wired into generic root and parallel execution, reads, outputs, joins, and recovery | `variable_turn` reloads pure replay before each append, validates the immutable graph and exact active work, resolves exact artifacts, reducer-validates the envelope, and CAS-appends one payload. CAS conflicts reload and reclassify; initialization and node-output prefix recovery commit only missing events. `TurnLogic` suppresses an exact already-committed output prefix before node completion. The parallel coordinator applies the same work/input discipline to branch output and records shared contributions for deterministic join merge. Production adapters confine persistence and artifact inspection to runtime data ports. |
| Durable tool approvals | End-to-end validated | Session-scoped checksum-protected pending-action records, four-layer CLI resolution, continuation-resume interception, mandatory-policy revalidation, daemon restart before approval, no pre-approval execution, approved dispatch, structured denial, canonical model continuation, and duplicate-resolution idempotency pass `tests/e2e/durable_tool_approval.ps1` on Windows. Replay retains bounded terminal outcomes and exact action digests, repairs an absent or call-only provider conversation pair without redispatch, and rejects mismatched digests, reversed pairs, and conflicting history; canonical dispatch outbox plus request-bound terminal receipts reconcile post-dispatch crashes without re-execution in `runtime_tool_receipt_recovery.ps1` |
| Deterministic harness mock provider | Integration tested | Full harness N-tier mappings; text, streaming, tool-call, malformed, timeout, rate-limit, partial-failure, cancellation, usage and disconnect scenarios |
| Runtime action interception | Implemented and unit tested | Style-before-plugin ordering, exact replacement audit, final-proposal permission evaluation and mandatory-deny enforcement |
| Deterministic compaction strategies | All built-in strategies execute live; typed summary and artifact handoff process-tested on Windows and Ubuntu/WSL2 | Typed summary builds a deterministic bounded JSON projection from complete typed entries, preserves required live records, reaches the provider as `ContextSummary`, retains canonical history, and replays without recomputation. Artifact handoff persists the exact complete projection/provenance document through a canonical policy/outbox/replacement-approval lifecycle, binds logical and content-addressed identities into projection and event provenance, reconciles exact restart cuts, verifies retained content before later live effects, and leaves pure replay storage-free. `runtime_typed_summary.ps1`/`.sh` and `runtime_artifact_handoff.ps1`/`.sh` prove the real runtime/harness/CLI paths, daemon replacement, exact retained plans, unchanged history, and pure replay. |
| Native process host | End-to-end validated | Authenticated foreground/background execution plus native PTY start/run, interactive input, resize, detach/reattach, merged durable terminal output, restart-persistent replay denial, strict owner/session scope, secret references, executable policy, and bounded concurrency/waiters pass. Recovery records commit before dispatch, bind PID + OS start time + resolved executable, reject PID reuse, preserve completed output, classify exact surviving children without redispatch, and quarantine malformed records. The host exposes authenticated versioned Unix-socket/Windows-pipe transport, survives runtime-client replacement in a separate process group, retains live PTY handles across reconnect, and exits after its last live child and request. A real Windows daemon E2E starts one PTY, kills and replaces the runtime, reattaches through the surviving host, exchanges canonical input/output, commits exit, and proves no redispatch; equivalent Unix automation is present. Typed `process.reconciliation_started/completed` events form one reducer-enforced pair around reattachment and precede terminal tool state. A separate forced host-crash test proves no redispatch and fail-closed inherited-handle recovery |
| Native filesystem host | Integration tested | Separate N-tier host with bounded read/list/glob/grep, atomic write/edit, prevalidated multi-file patch, encoding/binary handling, lazy schemas and path/symlink/device/sensitive-file controls; 13 tests |
| Plugin manifest SDK | Implemented and unit tested | Strict TOML/JSON model, PLUG001–PLUG032 validation, authority/trust/capability/version checks, exact plugin node-executor identity/API/schema/timeout/failure/idempotency/permission/state-scope/effect declarations, and cross-plugin ordering diagnostics; 13 tests |
| Runtime plugin composition | Observer composition, interceptors, node executors, context transforms, automatic memory writes, memory retrieval/compaction, correlated live cancellation, lifecycle management, and idle teardown process-tested on Windows and Ubuntu/WSL2 | Runtime dependency supervises keyed plugin-host sessions; data compiles exact SDK-validated manifests, owns catalog normalization/activation, and computes the plugin-set hash; logic composes immutable declarations and admits only typed, runtime-validated outcomes. Protocol v10 carries exact authenticated cancellation and lifecycle identity. Blocking/node/context/memory operations retain their existing canonical proposal, dispatch, receipt, validation, and recovery paths. Observer delivery uses canonical Proposed/Dispatched/terminal events and durable exact worker receipts; startup reconciles only replayed pending identities under strict bounds. The composition process suite proves interceptor replacement, real filesystem execution, canonical observer proposal/dispatch/completion, observer worker delivery, inspection, daemon restart, and a second post-restart session on both operating systems. Enable/unquarantine and exact pending-lifecycle startup reconciliation are live. Idle host teardown requires every canonical, host, durable-state, and transport operation class to be quiescent and is process-tested on Windows and WSL2. Blocking-interceptor reject/failure outcomes now have bounded canonical reducer validation, retain exact input/output hashes, and replay before a later same-session invocation in focused tests; a dedicated rejected/failed process matrix remains pending. |
| Plugin-provided node executors | Generic `TurnLogic` and isolated process paths validated on Windows and Linux; exact invocation, parallel application, state, lifecycle preemption, and recovery are canonical | Validated plugin declarations feed the single immutable node-executor registry, with exact declaration hashes retained in execution-plan schema v2. Runtime logic owns bounded proposed→authorized→dispatched→completed/failed/ambiguous events and never automatically reinvokes a replayed dispatch. Completed proposals pass runtime-owned schema, transition, variable, artifact, action, budget, and preserved-state validation. Session state reads use exact replay-derived generation/hash identity; nonempty state writes traverse runtime logic→data→dependency→plugin host and commit a hash-only receipt before node completion. Keyed nonce-bearing grants and bounded process frames remain enforced across the plugin-host N-tier boundary. The nine-case isolated executor suite proves direct invocation, reconstruction, durable action approval, state, validation, timeout, unavailability, and receipt-only recovery. The Windows and Ubuntu/WSL2 Graph C process matrix now also admits an exact plugin-host executor inside a parallel region, carries its declared action and persisted artifact into the canonical join, and proves correlated cancellation, terminal receipt evidence, duplicate suppression, and no restart redispatch. Legacy plans lacking declaration hashes fail with `NODEX107` and require an explicit recompiled branch. Additional runtime-action classes beyond the validated declared tool/network set and nested parallel regions remain incomplete. |
| Runtime plugin lifecycle management | Full daemon/CLI E2E validated on Windows and Ubuntu/WSL2 | `runtime_plugin_lifecycle.ps1` and `.sh` run disable→enable and quarantine→unquarantine through CLI → runtime service → logic → data → dependency → plugin host → isolated worker. Exact plugin/version/configuration/action/cancellation identity is canonical before host I/O; the host persists an exact terminal receipt. Same-identity retries are receipt-only, inactive plugins reject later work, and in-flight non-idempotent work is cancelled without a late effect. A crash after the host receipt but before the canonical changed event is reconciled at daemon startup without changing action or cancellation identity. Legacy pending records without an exact cancellation identity fail closed and require explicit migration. |
| Canonical plugin observer delivery and idle teardown | Coordinator/reducer tests plus Windows and Ubuntu/WSL2 composition and process-teardown proof | Observer delivery persists exact plugin/version/handler/declaration/configuration/event-range/request/cancellation identity and reduces Proposed → Dispatched → Completed, Rejected, Failed, or Ambiguous. Queue acceptance is never completion. Missing host receipts are sealed ambiguous once and never redispatched; exact terminal receipts replay. Startup recovery scans only replayed pending identities under session/delivery/journal-byte bounds and fails closed on missing or substituted history. The real composition suite commits exact Proposed/Dispatched/Completed events, delivers the subscribed canonical event to the isolated observer, exposes replay state through inspection, restarts the daemon, and succeeds again on Windows and WSL2. Host health binds active invocations, queued/active observers, and flushed durable state; runtime teardown also requires no pending canonical observer, continuation, lifecycle/transport waiter, node/context/memory/compaction/state/interceptor operation. The separate real process test proves pending work blocks teardown and an exact terminal receipt permits host exit on both systems. |
| Harness continuation gate | Integration tested | Tool-call generation stops at a proposal; explicit runtime continuation issues a fresh provider request exactly once, with replacement structured context |
| Native Git host | End-to-end validated | Discovery/status/diff, detached worktrees, commit-free integrity-checked checkpoints and guarded restore have 9 host tests; runtime routing uses keyed grants and `tests/e2e/runtime_git_loop.ps1` validates a real repository status round trip |
| Native LSP host | End-to-end validated | Separate five-crate host implements LSP 3.17 lifecycle, all required query/edit-proposal operations, cancellation, timeout, restart, workspace containment, keyed authorization and deterministic fixture coverage; runtime project-root routing passes a process E2E |
| Session-style SDK | Implemented and unit tested | Five built-in semantics and thirteen exact immutable versions, strict owned TOML/JSON manifests, STYLE001–STYLE043 validation, graph/pipeline/harness compilation, availability/budget checks, inspectable descriptors and compatibility-bound cache keys. The SDK version table is the authoritative historical catalog: lookup is exact and latest selection cannot silently substitute a persisted version. Plugin memory-provider and compactor selections bind exact plugin/component IDs, semantic versions, declaration hashes, operation availability, configuration references, allowlists, and compaction strategy; compatible substitution and ambiguous catalog matches fail validation. `planner-worker@1.4.0` extends the generic parallel/review graph with evidence-aware executor versions, runtime-owned branch workspaces, child tool groups, and exact child artifact/diff/test evidence; earlier planner-worker versions remain available without rebinding. Exact Persistent, Research, and Planner-worker v1.1 sources and hashes remain frozen for legacy recovery. |
| Runtime session-style registry and binding | End-to-end validated on Windows and Ubuntu/WSL2 | Runtime dependency discovers bounded user/project/plugin TOML/JSON sources and disable markers and persists compiled cache records; data compiles through the SDK and owns catalog/cache records; logic owns exact ID/version selection, compatibility, immutable binding, component/budget override compilation, and fail-closed restart validation; service exposes style/component list, inspect, validate, and compile plus binding creation/branch operations. Complete identity, manifest, compiled descriptor, memory/compaction/tool/harness/budget/permission selections are canonical and atomic in schema-v2 session metadata, `style.json`, and `style.lock`. Discovery exposes all eleven exact versions without a second version catalog. Current Windows and Linux `runtime_style_registry` process runs prove post-turn daemon replacement retains the complete Persistent v1.2 binding and execution plan byte-for-byte, executes again without rebinding, restyles a branch explicitly, and refuses fallback after disablement. The scripts now also assert every exact catalog identity and byte-identical restart retention for `persistent-chat@1.1.0`; those updated assertions await the next recorded process run. `runtime_tui_smoke.ps1` proves the four-layer TUI branch and component/budget-selection paths. |
| Runtime node-executor registry and immutable execution plan | Implemented and unit tested | The single runtime capability registry deterministically resolves every compiled node and persists one hashed execution plan in the immutable session-style binding before session, branch, and child creation. Exact executor ID/version/source/boundary, plugin identity, executor-declaration hash, runtime API requirement, required/resolved capabilities, node-configuration hash, registry hash, and compilation identity are revalidated at style compatibility and turn preflight without rebinding. New plans retain compiler generation `agentmod-runtime-node-plan@3`; exact `@2` and `@3` plans revalidate, while unknown generations, legacy planless bindings, and v1 plans without declaration hashes fail with explicit migration diagnostics. `style.execution_initialized` retains the complete replay contract. Focused tests prove arbitrary resolved topology acceptance, plan serialization round trip, both supported compiler generations, registry/version/declaration drift rejection without mutation, contract tamper rejection before append, and pure replay of the initialization contract. |
| Generic runtime session-style executor | Arbitrary Graphs A/B/C plus all current built-ins validated across Windows/Linux process paths | Runtime logic consumes the exact SDK-compiled graph and persisted node plan retained by the immutable session binding, verifies all cache/plan/node-configuration identities, and derives every cursor handler from the exact resolved executor rather than style or node name. A logic-owned typed execution contract returns bounded completed/waiting/spawned/parallel/retry/failed/emitted/terminal outcomes. Arbitrary non-profile graphs execute through exact persisted native or plugin IDs. Initial graph variables are bounded, canonicalized, reconstructed from replay, and changed recovery inputs fail before append. Exact reducer evidence binds generic context, model, child-message, parallel-root, join, and plugin completion to canonical boundary or receipt evidence. Graph A/B/C, migrated built-ins, and current Planner-worker v1.4 pass process tests on Windows and Ubuntu/WSL2. Current Planner-worker adds overlapping child turns, runtime-owned branch workspaces, child tool/result artifacts, deterministic integration, and evidence-bound review without an adapter-profile gate. Exact Research and Planner-worker v1.1 remain executable through frozen legacy adapters. |
| Durable generic graph wait and scheduling | Unit/integration tested and one-time user-schedule process validated on Windows/Linux | Exact `runtime.delay@1.0.0` and `runtime.schedule@1.0.0` handlers resolve runtime time once into canonical `graph.schedule_*` outbox events, persist an exact scheduler request and typed `GraphNodeWait`, and resume through the existing scheduler claim and continuation systems. The continuation binds session/run/branch/node/counters, exact executor ID/version/source/boundary, node-configuration and execution-plan hashes, transition, schedule, and cancellation references. Wake revalidates every retained executor/branch/transition field against the immutable plan and current canonical cursor before continuation claim, then validates the exact typed resume returned by the CAS. Nonwaiting one-time, recurring, runtime-event, and process-output registrations deliver authenticated claims through a runtime-logic-owned use case: it revalidates the immutable plan and exact stored graph schedule, commits a bounded typed `graph.schedule_triggered` receipt with exact execution/time/observation provenance, and deduplicates restart delivery by execution ID without synthesizing a user/provider turn or mutating variables. Recovery completes an existing trigger receipt without redispatch and safely retries a claim with no canonical receipt. Expiration is durably transitioned before failure. The compiled `CancelContinuation`, `SuppressEffects`, `CancelTrigger`, or `CancelWaitOnly` behavior is retained in replay; cancellation commits intent and scheduler-side receipt events, removes only when declared, terminal-transitions the continuation when declared, and commits one canonical cancelled wait. Duplicate wake and cancellation calls are no-ops; substituted executor/transition/plan payloads and policy denial fail closed. Consequential schedule creation traverses style/plugin interceptors plus user and mandatory policy; Ask creates a durable approval continuation, revalidates mandatory policy after approval, and dispatches the exact action digest once. Unit tests cover delay restart/wake, duplicate wake, approval/denial, cancellation, expiration, and all four nonwaiting trigger classes; a dispatch-before-receipt crash cut proves exact scheduler reconciliation. A dedicated arbitrary user graph now proves a configured one-time `schedule` node on Windows and Ubuntu/WSL2: exact `runtime.schedule@1.0.0` plan/configuration identity, policy-approved action digest, durable scheduler storage, daemon replacement before wake, one resumed continuation and terminal marker, no duplicate claim/effect, immutable style lock, and append-free pure replay. Cancellation outbox crash-gap process tests remain pending; recurring, runtime-event, and process-output graph-node process coverage remains unit/integration only. |
| Generic child-agent messaging | Runtime/data/dependency integration and Graph B Windows/Linux process execution tested | The available exact `runtime.child-message@1.0.0` executor resolves from the immutable node plan and commits canonical parent Proposed → Approved → Dispatched → Delivered/Failed/Ambiguous state. The proposal binds exact node-work identity, child ownership/link and lifecycle, typed payload, message sequence, security classification, bounded artifact references, variables, and plan hash. Style/plugin interceptors plus user and final mandatory policy run before dispatch; Ask uses a durable child-message approval continuation, mandatory policy is revalidated after approval, and duplicate approval/denial is an exact no-op. Runtime seals the typed `child_agent.message_received` child event and the existing child `events.jsonl`/catalog data path atomically appends or exact-replays it under a per-child operation lock; the receiving child retains it in a dedicated replay projection and does not fabricate conversation input. Recovery completes a missing parent receipt only when the exact sealed child event exists. A dispatched parent intent with no matching child event becomes canonical ambiguous state and is never redispatched. Focused tests cover exact and declared typed-variable selection, ownership/lifecycle/cancellation/security/size/artifact validation, fresh and replayed receipts, changed-input conflicts, direct generic dispatch, approval/denial and duplicate resolution, dispatch crash reconciliation, and ambiguous no-redispatch. Strict Graph B process runs on Windows and Ubuntu WSL2 prove exact first and revised child identities, canonical delivery without fabricated user messages, parent restart, idempotent recovery, and terminal replay. |
| Generic child spawn/wait/review application | Live in normal generic Turn and restart-tested by Graph B on Windows and Linux | The style-independent runtime planner maps exact persisted `runtime.child-spawn@1.0.0`, `runtime.child-wait@1.0.0`, and `runtime.review@1.0.0` identities into authoritative child state. The production composition root supplies the existing journal, child-session, continuation, interception, permission, provider-review, and ancillary-application boundaries to `TurnLogic`. Child creation retains typed proposal/outbox/receipt hashes through Proposed → Approved → Dispatched → Created → Completed/Failed/Cancelled. Wait replay retains stable successful, unsuccessful, pending, cancellation, timeout, and impossible-threshold sets and selects reused revision task IDs by exact child session identity. Accepted timeout cancellation now follows canonical Requested → Authorized → Dispatched → Completed/Ambiguous state, binds the exact child set and branch work, and transitions each exact child through the production session boundary without fabricated user input. Every append cut is unit-tested for receipt recovery and ambiguous no-redispatch. Immutable branch/plan/work/node-config/adapter-config/cancellation validation occurs before the approval continuation CAS; duplicate, reordered, cross-branch, and substituted identities fail closed. Review replay validates approved/revision/failure destinations and structured provider evidence; deterministic UUID cancellation identities satisfy the harness boundary. Strict Graph B Windows and Ubuntu WSL2 runs prove the complete successful orchestration path plus a separate accepted-cancellation Ask/restart path with two exact child receipts, terminal replay, and no duplicate dispatch. |
| Generic parallel/join state engine | Native registry admission, normal generic `RunTurn`, every-prefix recovery, and Graphs A/B Windows/Linux process execution tested | Exact `runtime.parallel@1.0.0` and `runtime.join@1.0.0` are available in the single native registry. Admission derives regions structurally from labeled fan-out edges and the typed join target, rejects malformed/nested regions, plugin-host branch nodes, and native branch executor classes without production ports using stable `NODEX008`/`NODEX009` diagnostics, and persists/revalidates the exact plan without style or fixture classification. The bounded state engine retains stable branch/dispatch IDs, queue/backpressure state, independent control, cancellation, join timeout/readiness, shared-variable contributions, deterministic merges, and ordered result/artifact projection. The outer coordinator reducer-validates each CAS append, records effect outbox before dispatch, reconciles terminal receipts, and does not redispatch ambiguous or completed effects. Normal admitted non-built-in graphs pass `TurnLogic::run_turn` for pure parallel/join completion and approval+artifact waiting/restart/resume with typed outputs and duplicate suppression. Focused parallel tests cover every outer prefix, append conflicts, bounded scheduling, waiting/cancellation/ambiguity, fresh sequential branch inputs, shared merges, effect receipt recovery, and inheritance of the parallel owner's loop identity by initial and recovered branch work. Strict Graph A process tests on Windows and Linux prove parallel tool/artifact branches and generic join after restart; strict Graph B runs on both systems prove parallel child waits, revision-loop identity, join readiness, structured review routing, and replay. Plugin-host executors remain deliberately prohibited inside parallel regions until their effect outbox can be owned per branch; Graph C proves generic plugin dispatch outside such regions. |
| Ephemeral-turn style execution | Generic v1.2 end-to-end validated on Windows and Ubuntu/WSL2; exact v1.1 compatibility retained | The versioned SDK graph `fresh-context → respond → tool-batch → done` executes every node through its exact persisted `runtime.context-construction`, `runtime.model-request`, `runtime.tool-gate`, or `runtime.turn-completion` resolution. Each turn authorizes and canonically records one current-turn-only provider projection, retains typed canonical user/assistant history without fabricated handoff messages, and authorizes a phase-bound empty projection before completing the turn. Exact graph-edge, run, request, provider, model, options, and input identities govern recovery. A provider-completion receipt is persisted before canonical response commitment; restart completes the same exact invocation without provider redispatch. Journal-cut tests cover fresh replacement, context-to-model transition, assistant commit, discard phase, and discard boundary. The Windows and POSIX `runtime_ephemeral_turn` process suites prove receipt-window daemon replacement, two isolated turns across restart, exact generation-3 plan/registry/executor contracts, empty dormant projection, complete canonical history, no turn-one leakage, pure replay, and explicit `ephemeral-turn@1.1.0` execution without rebinding. Runtime discovery exposes every exact built-in version declared by the SDK, so historical sessions are reachable without a second version catalog or fallback selection. |
| Persistent-chat style execution | Generic v1.2 end-to-end validated on Windows and Ubuntu/WSL2; exact v1.1 compatibility retained | The current `prepare-context → respond → tool-batch → done` graph executes exact persisted context, model, tool-gate, and turn-completion executors under plan compiler generation `@3`. Preserve-history context and preserve-projection completion retain canonical user/assistant context across turns. The provider batch admits the complete native tool catalog, normalizes declared legacy provider aliases to canonical IDs before allowlist checks, and uses the normal proposal/policy/grant/receipt/continuation path. Runtime unit tests prove the generic four-node path and staged alias approvals; exact v1.1 still selects `PersistentTurn` legacy recovery. Current Windows and Linux `runtime_cli` runs prove exact executor identities, registry/plan contract hashes, canonical provider lifecycle, and retained projection. Current Windows and Linux `runtime_style_registry` runs replace the daemon, compare the complete immutable binding before/after, execute again, branch explicitly, and reject disabled-style fallback. |
| Research-loop style execution | Generic v1.2 and frozen v1.1 end-to-end validated on Windows and Ubuntu/WSL2 | Current `research-loop@1.2.0` compiles `fresh-context → research → tool-batch → persist → repeat → done` into six exact V3 executor resolutions and runs three iterations through generic dispatch. Each fresh context/model pair retains a distinct exact work identity; model disposition/result, provider receipt, artifact, and loop state are typed canonical variables. Artifact persistence consumes the exact canonical provider-result reference and writes provider-visible text through the normal proposal/policy/outbox/receipt path. Locked Windows and Linux `runtime_research_loop` process runs assert exact plan/registry contracts, 16 node completions and 15 transitions, three model bindings, three physical artifact contents, canonical event counts, daemon restart with byte-identical binding/style/journal state, and pure replay. The same matrix explicitly selects frozen `research-loop@1.1.0`, retains its historical adapter behavior and three findings, and proves restart/replay without rebinding. |
| Declarative-graph style execution | Generic v1.2 end-to-end validated on Windows and Linux; exact v1.1 legacy recovery unit tested | Compiler-generation-aware routing checks typed graph semantics before adapter classification. New declarative v1.2 sessions persist an `agentmod-runtime-node-plan@3` plan and execute every conditional, approval, tool, loop, and terminal node through its exact selected runtime executor. Runtime-produced request and tool-argument variables and the loop-produced iteration variable are canonical, typed, versioned, and replayable. Approval and `filesystem.read` use the normal proposal/policy/grant/host/receipt paths and retain generic result references; reducer evidence binds completion to exact executor receipts. Exact v1.1 and compiler-`@2` histories remain on the versioned legacy adapter and are never silently rebound. `runtime_declarative_graph.ps1` and `.sh` both pass actual process execution, proving exact plan/hash identity, both branches, three tool effects, restart at approval, duplicate-resolution idempotency, terminal inspection, absence of legacy evidence in v1.2, and pure replay. The Unix script uses an isolated target directory and explicitly builds/wires the scheduler, so the Linux result does not depend on ambient binaries. |
| Runtime-managed planner/worker/reviewer and child sessions | Generic v1.4 and frozen v1.1 end-to-end validated on Windows and Ubuntu/WSL2 | Current Planner-worker v1.4 persists exact evidence-aware executor resolutions and runs plan, two parallel child branches, exact join/integration, artifact persistence, structured reviewer rejection, bounded revision children, and terminal completion through the generic dispatcher. The cross-platform `runtime_planner_worker_1_4` suites hold both first-pass children concurrently at independent harness gates, release them in reverse task order, and prove deterministic integration. Each child receives a distinct runtime-owned `branch_workspace` lease with `manual_review`, executes canonical filesystem edit, process test, and Git diff calls once, and persists child-owned result/diff/test artifacts. Parent integration and reviewer output consume exact artifact references; the parent workspace remains unchanged. Daemon replacement, duplicate suppression, and structured revision then approval are covered; the prior v1.3 matrix retains pure-replay evidence. Exact v1.1 continues through its frozen adapter. Workspace modes other than the proven branch-workspace path and the full write-denial matrix remain incomplete. |
| Style-selected context, memory, and compaction | Built-in compaction/write paths, native and plugin automatic-memory Ask, exact iteration/session-completion writes, plugin retrieval/compaction, and plugin context transforms process-tested on Windows and Ubuntu/WSL2 | The SDK persists bounded retrieval, write, compaction, preservation, projection, and exact ordered plugin-transform selections. Runtime data routes no/file/SQLite memory with checksum, scope, item, byte, and token bounds. Context phases are canonical, dynamically ordered, request-bound, and replayable. Summary and artifact-handoff compaction execute live. Plugin retrieval, compaction, transforms, and automatic writes use immutable exact declarations, runtime-owned outboxes, sealed receipts, bounded validation, and mandatory policy revalidation. Native automatic-memory Ask uses its own continuation payload, identity domain, and exact action-digest approval subject; plugin continuation serialization and IDs remain unchanged. Windows and Ubuntu/WSL2 process tests prove pending restart, one approved dispatch, effect-free duplicate approval, and denial without dispatch for native Ask, alongside the existing plugin Ask and crash-recovery matrices. Every retained automatic-memory conversation entry carries a runtime-owned information-flow class and bounded credential/path/URL/handle detection rejects the whole projection before proposal. Broader semantic DLP remains incomplete. |
| Runtime harness registry and per-session selection | End-to-end validated on Windows and Ubuntu/WSL2 with a truly independent second harness executable | Runtime dependency owns an injected adapter registry and cancellation route, data normalizes catalog records, logic computes capability hashes and validates exact style requirements, and service exposes list/inspect plus create-time selection. Immutable style bindings, model proposals, action digests, grants, and canonical model-request events retain harness identity. Native and independently supervised deterministic fixture entries prove routing is not hard-coded; the fixture intentionally lacks image support for negative compatibility coverage. CLI supports `harness list`, `harness inspect`, and `session create --harness`; the command-driven TUI catalog selects a harness during `/new`. `agentmod-independent-harness-fixture` is a separately compiled test-only executable whose only AgentMod crate dependency is the public harness protocol; it validates the runtime keyed grant at its own dependency boundary, enforces nonce replay and frame/request bounds, and emits a distinct deterministic event stream. It imports none of `apps/harness` service, logic, data, or dependency. The Windows and hermetic Ubuntu/WSL2 `runtime_harness_selection` suites prove discovery, compatible native/independent turns, incompatible creation rejection, durable identity, canonical audit, exact independent output, restart validation, and exact independent post-restart execution. Each suite also rejects a Cargo dependency tree containing any native harness internal crate. Complete third-party production adapters remain planned. |
| Runtime style/graph introspection | End-to-end validated on Windows and Ubuntu/WSL2 for the research replay/restart projection; canonical-variable extension unit tested | Runtime logic builds a bounded inspection projection solely from the immutable binding and canonical replay state; service adds it to session inspect/replay without invoking effects. It exposes style/source/cache and harness identity, complete compiled nodes/edges, control/active/previous nodes, known next transitions plus conditional candidates, loop/retry counters, exact remaining step/token/iteration budgets, tool/permission/retry/termination configuration, blocking pipeline order and invocation history, memory provenance, compaction boundaries, child executions/joins/reviewer findings, and termination. When replay contains canonical variables, introspection evaluates each retained compiled condition and reports exact `eligible`, `ineligible`, `missing_input`, or `invalid_expression` status. The bounded variable projection contains type/scope/producer/consumer/mutability/merge/size/classification metadata, assignment state, version, and value hash but never values or secret references; reducer serialization/restart reconstructs identical metadata and transition classifications. Legacy snapshots without the canonical projection retain conservative conditional candidates. Three focused tests cover all classifications, legacy fallback, restart equality, and secret-value non-disclosure. Cost and elapsed-duration remainder are explicitly unavailable because canonical accounting does not yet retain them. The TUI fetches this projection on selection and material lifecycle events and renders a Graph view. The cross-platform research-loop E2E proves three-iteration projection, restart, and pure replay; the TUI smoke E2E still passes. |
| Runtime local RPC transport | Integration tested | Protocol 2.5 adds session-scoped plugin disable/quarantine requests and exact lifecycle responses while retaining bounded framed negotiation, mandatory bootstrap-token authentication, concurrent local socket/named-pipe connections, request dispatch, style/harness/component discovery, optional session component and budget selections, ordered `StreamItem`/`StreamEnd` frames, committed-sequence binding, and bounded channel backpressure tests. A terminal-only style turn is explicitly framed as `StreamEnd` even when it emits zero provider events |
| Runtime↔harness durable turn | End-to-end validated on Windows and Ubuntu/WSL2 | CLI create/run traverses authenticated local transport, runtime replay/commit, ordered interception and policy, short-lived keyed grant, supervised harness, deterministic provider, canonical proposal/approval/started/delta/completion events, and assistant commit. Current `runtime_cli.ps1` and `.sh` process runs use Persistent v1.2 and assert exact plan/compiler/registry/executor identities plus four generic node completions and retained projection. |
| Runtime provider stream cancellation | End-to-end validated | Harness lifecycle events cross the process boundary as individual bounded frames, are committed one at a time, and cross runtime RPC as ordered bounded stream frames; caller-selected cancellation IDs travel through four-layer CLI/runtime mappings, active cancellation interrupts and drops the harness child, partial visible output and cancellation are committed without completion, and a fresh request reconnects in `runtime_stream_cancel.ps1`; Unix automation is present |
| Live headless CLI turn stream | End-to-end validated | `agentmod run --stream-json` maps bounded stream types through dependency → data → logic → service, validates RPC identity and monotonic frame order, flushes each NDJSON event only after its canonical commit, and ends with one sequence-bound terminal record; `runtime_cli_stream.ps1` proves the first frame is observable before provider completion and Unix automation is present |
| Credit-window flow control and reconnect | End-to-end validated | Runtime protocol 2.1 negotiates request-bound credit windows; the server emits one initial nonterminal frame and blocks until an exact sequence acknowledgement grants more capacity. `agentmod session events <id> --after <sequence> --limit <n>` scans verified canonical history into bounded pages, reports stable cursors/head/`has_more`, and `runtime_session_reconnect.ps1` proves pages 1–19 contain no gaps or duplicates, including the style graph events; Unix automation is present |
| Cross-host terminal receipts | End-to-end validated | The supervised runtime dependency atomically persists checksum- and exact-request-bound terminal event streams before returning them to logic; receipt-only recovery skips an already committed host-event prefix and refuses missing/corrupt/conflicting receipts. A forced daemon kill after a real filesystem write and receipt but before terminal journal commit recovers with the filesystem host deliberately unavailable, proving no redispatch; Unix automation is present |
| Startup-wide tool reconciliation | End-to-end validated | Before opening RPC, the runtime service scans every verified receipt, reduces the corresponding canonical journal, reconciles ordinary nonterminal dispatches from the receipt without spawning the disabled host, and classifies already-terminal, orphaned, and approval-owned receipts. `runtime_startup_tool_recovery.ps1` proves the recovered session can continue; approval-owned receipts remain with the resume-once continuation path, and Unix automation is present |
| Runtime filesystem tool loop | End-to-end validated | `tests/e2e/runtime_tool_loop.ps1` passes CLI → named-pipe runtime → keyed harness proposal → ordered runtime authorization → keyed per-session filesystem process → structured tool result → explicit harness continuation; Unix automation is present |
| Runtime process tool loop | End-to-end validated | `tests/e2e/runtime_process_loop.ps1` passes CLI → runtime → harness → ordered authorization → keyed per-session process host → captured output/result → explicit continuation; executable policy is deny-by-default and Unix automation is present |
| Multi-tool batch join | End-to-end validated | One provider response can propose multiple safe tool calls; runtime commits and executes each result before one resumed provider request, while the harness resolves all sibling continuation aliases as one batch. Windows `runtime_multi_tool_loop.ps1` passes and a Unix script is present |
| Extended runtime host routing | End-to-end validated | Git status, offline Web search, LSP project-root detection, MCP server listing, and a configured external MCP stdio invocation traverse CLI → runtime → harness proposal → policy → isolated host → canonical progress/result → provider continuation in dedicated Windows E2Es; Unix scripts are present |
| Autonomous coding loop | End-to-end validated | `tests/e2e/coding_task.ps1` reads a real Rust project, performs an intentionally incomplete edit, records a failing `cargo test`, fixes the source, records a passing test, and independently re-runs the final test; Unix automation is present |
| Durable session catalog | Integration tested | Runtime endpoint creates the complete required directory atomically with initial canonical event and lists bounded dormant metadata without loading conversation state |
| Replaceable memory providers | End-to-end validated for file and `SQLite` crash recovery on Windows and Ubuntu/WSL2 | No-memory, checksum-protected file memory, and bundled `SQLite` FTS5 ranked retrieval pass dependency/data/logic tests; unapproved writes are rejected before data access and injection provenance is complete. Exact writes use deterministic provider-local identities. Runtime-owned turn-completion writes bind immutable style provider/scope/policy plus request/content/timestamp identity, while iteration writes additionally bind the exact successful loop transition and use a separate v2 digest domain. Both traverse policy and mandatory revalidation and commit a replay-owned outbox. Focused tests cover configuration substitution, request substitution, proposal ambiguity, mandatory denial, completed replay, dispatched reconciliation, boundary substitution, duplicates, and legacy v1 stability. `runtime_automatic_memory.ps1`/`.sh` and `runtime_iteration_memory.ps1`/`.sh` provide real file process crash evidence. `runtime_sqlite_memory_crash.ps1`/`.sh` prove the `SQLite` post-commit/pre-canonical-completion cut, one logical row, one terminal lifecycle, poisoned-harness recovery without model redispatch, semantic database stability, and pure replay on Windows and Ubuntu/WSL2. |
| Native Web host | End-to-end validated | Separate N-tier host implements authenticated HTTP, HTML fetch, deterministic/Brave search, dependency-reconstructed action grants, restart-persistent replay denial, per-hop DNS/domain/private-IP/redirect policy, secret references, cancellation, bounded projections/concurrency and atomic artifact overflow; offline runtime search routing passes a process E2E |
| Native browser host | End-to-end validated | Separate five-crate WebDriver host implements lifecycle, rendered navigation/inspection, final-URL policy, screenshot/download artifacts, CSS click/type/form submission, health, cancellation, keyed grants, durable replay denial and shutdown. A real compiled WebDriver fixture drives nine operations through runtime and provider continuation in `runtime_browser_loop.ps1`; Unix automation is present |
| Durable scheduling | End-to-end validated | A separate five-crate worker and dedicated protocol implement authenticated negotiation, checksum-protected one-time/recurring/runtime-event/process-output schedules, portable atomic replacement recovery, complete execution policy, deterministic create-once claims with claim timestamps, idempotent terminal markers, corruption rejection and restart deduplication. Runtime-event and process-output observations carry their source session through the complete runtime/scheduler N-tier path; the scheduler filters schedule ownership before claiming, and each durable claim retains the exact event or output ID across restart. Runtime continuation proofs and canonical `scheduler.fired`/resumed graph-wait events retain that exact observation, while recovery rejects mismatched provenance. Runtime, CLI, and TUI have layer-local management mappings; the daemon automatically polls time triggers and observes newly committed canonical ranges for exact event IDs and process/log-stream byte ranges. Typed deferred turns are bound to an exact session, schedule and trigger proof, reject manual approval, enforce expiry against the durable claim time, transition resume-once, and then re-enter the normal intercepted provider path. Startup enumerates durable nonterminal claims: claims without canonical dispatch provenance are safely resumed, canonical terminal outcomes are reconciled without redispatch, and ambiguous in-flight work fails closed. A canonical `scheduler.delivery_reconciled` event precedes the worker terminal marker so recovery itself is repeatable. `runtime_scheduler.ps1` and `.sh` now use the TUI to create/list/remove the due schedule and pass on Windows and Ubuntu/WSL2 while also proving deferred wakeup; `runtime_scheduler_recovery.ps1` kills the daemon before dispatch and again after canonical model completion, then proves exactly one provider execution, reconciliation event, and terminal marker. Cross-session filtering, duplicate observation idempotency, and restart-preserved observation identity are covered by scheduler dependency and runtime service tests. |
| Native MCP host | Partially implemented | Five-crate process implements initialization/version negotiation, lazy tools/resources/prompts discovery, namespacing, stdio and Streamable HTTP, session IDs, progress, cancellation, shutdown, inert managed catalog, deterministic mock and real compiled stdio fixture. Negotiated HTTP calls and resumptions send the required `MCP-Protocol-Version` header. Multi-event SSE and bounded resumption carry exact session/event cursors. Checksum-protected HTTP recovery state binds server configuration, runtime owner/session, negotiated version, MCP session, cursor, pending JSON-RPC request and normalized operation digest; a reconstructed dependency resumes the exact GET stream without a duplicate POST, rejects cross-operation/cross-server reuse, suppresses the prior cursor, and atomically clears pending state on terminal result. The MCP 2025-11-25 authorization profile is live through a hidden authenticated CLI/runtime management route: RFC 9728 and RFC 8414/OIDC discovery, authorization code with PKCE S256, protected-resource binding, a dependency-owned loopback callback, encrypted PKCE/token secret references, exact refresh, cancellation, restart reconstruction, and ambiguous exchange/refresh fail-closed state. Canonical management audits contain only request/configuration/redacted-result hashes; model discovery omits management endpoints. Focused real-loopback, ACP transport/branch, and immediate-child inheritance tests pass on Windows and Ubuntu/WSL2, including restart/replay deduplication and durable secret non-disclosure. Normalized resource-template descriptors, transitive/grandchild inheritance, and macOS execution evidence remain pending |
| Isolated plugin host | Integration tested | Five-crate process maps the versioned plugin protocol through all layers; SDK validation, keyed action grants, durable replay/state generations, approved executable roots, per-invocation crash isolation, timeout/cancellation/retry/rate limits, bounded observers and state changes are implemented; authority/cycle rejection has a real dependency test |

## In progress

- Additional plugin-proposed runtime-action classes beyond the validated
  filesystem/network actions and unsupported nested parallel regions.
- Broader semantic DLP for automatic memory.
- Planner workspace modes beyond the proven runtime-owned branch workspace and
  the complete workspace write-denial matrix.
- TUI LSP management remains unavailable pending a stable canonical runtime
  contract. Rich attachments, MCP OAuth, and read-only canonical artifact,
  child-agent, and process resource views are live.
- The current shared tree still requires the complete mandatory validation
  matrix to be rerun after final convergence; macOS process evidence is absent.

## Validation results

MCP OAuth process validation on 2026-07-31:

- Windows: after `cargo build --locked -p agentmod-runtime -p
  agentmod-mcp-host -p agentmod-cli -p agentmod-tui`,
  `$env:AGENTMOD_E2E_SKIP_BUILD='1';
  & tests\e2e\runtime_mcp_oauth.ps1` passed with
  `runtime MCP OAuth restart/audit process E2E passed`.
- Ubuntu/WSL2: `wsl.exe --cd /mnt/c/Users/jkpri/AgentMod sh
  tests/e2e/runtime_mcp_oauth.sh` performed the locked Linux build and passed
  with the same terminal result.

Both runs restart the runtime while the authorization transaction is pending,
reconstruct the dependency-owned loopback callback through a redacted status
request, complete the authorization-code exchange, and assert that canonical
history and durable host state contain no authorization URL, code, verifier,
access token, or refresh token. They also execute TUI OAuth begin/status/cancel
through an exact smoke-selected session, verify CLI-visible cancellation, and
read an empty canonical Runtime Resources projection. The existing Graph A,
Graph B, and process-restart suites now additionally use that same TUI path to
assert exact nonempty `resources=1/0/0`, `resources=0/4/0`, and
`resources=0/0/2` projections on both Windows and Ubuntu/WSL2. macOS was not
run.

The ACP-declared HTTP transport matrix also passed through real processes:

- Windows: `& .\tests\e2e\runtime_acp_mcp_http_sse.ps1` passed in 49.7 seconds
  with `runtime ACP Streamable HTTP and legacy SSE MCP E2E passed`.
- Ubuntu/WSL2: `wsl.exe bash -lc 'cd /mnt/c/Users/jkpri/AgentMod && bash
  tests/e2e/runtime_acp_mcp_http_sse.sh'` passed in 100.7 seconds with the same
  terminal result.

Both runs use real runtime, harness, ACP, and MCP-host processes plus loopback
fixtures. They prove Streamable HTTP session/version negotiation and
`Last-Event-ID` resume, legacy SSE same-origin POST discovery, successful
`mcp.invoke`, exact immutable session/load and substituted-header rejection,
in-flight disconnect cancellation, and recursive plaintext-secret absence.
The matching ACP branch process scripts also pass on Windows and Ubuntu/WSL2.
They copy the exact declaration into a freshly encrypted child bootstrap,
invoke the same external server before and after daemon restart, reject missing,
substituted, or unauthenticated source data, retain no plaintext secret, and
perform no MCP effect during pure replay.

The separate `runtime_child_mcp_inheritance.ps1` and `.sh` commands also passed
on Windows and Ubuntu/WSL2. Their style-wide explicit `inherit_mcp = true` path
copies the immediate parent's exact sanitized binding only through an `mcp` tool
gate; omitted/default and false policies bind no MCP servers. Parent bootstrap
authentication precedes child session-ID rewrite and fresh nonce, AAD, and
ciphertext. The fixture uses the returned child's exact immutable
`temporary_copy` workspace and canonical `invoke the inherited MCP fixture`
task. It proves exact child recovery, one MCP effect through execution and
daemon restart, and no duplicate on replay. Substituted declarations and
tampered parent or child bootstrap envelopes fail closed as
`InvalidConfiguration` with zero MCP effects. The workspace authorization
digest recursively canonicalizes nested JSON arguments at both runtime layers.
This evidence is limited to immediate children; it does not prove transitive or
grandchild inheritance, and macOS was not run.

`cargo deny check` passes after adding `chacha20poly1305`, `rand`, `sha2`, and
`zeroize` for MCP OAuth: `advisories ok, bans ok, licenses ok, sources ok`.
Existing policy-allowed duplicate-version warnings remain informational; no
license, source, advisory, wildcard, or duplicate exception was added for the
OAuth dependencies.

The arbitrary Graph A, child-orchestration Graph B, and plugin-node Graph C
process suites pass on both Windows and Ubuntu WSL2. Graph A proves all 14 exact
native executor identities, approval and delay restart cuts, parallel
tool/artifact execution, canonical variables, loop/event/join semantics,
duplicate suppression, and pure replay. Graph B proves the parent and all four
children retain compiler generation `agentmod-runtime-node-plan@3`, exact child
message identities, parallel waits, generic joins, review rejection, revision,
restart recovery, and pure replay. Its accepted-cancellation variant also
proves two durable Ask continuations across daemon replacement, two exact
child-session cancellation receipts, no fabricated child user input, and
unchanged dispatch/completion counts after terminal replay. Parent inspection
replays the failed terminal state and each child inspection replays its
cancelled lifecycle. The strict arbitrary-user Graph C scripts
pass from fresh builds on Windows and Ubuntu WSL2 for mixed runtime/plugin
dispatch, exact persisted declaration and execution-plan identities, lazy
canonical activation, invalid transition and malformed-output rejection,
restart and pure replay, timeout after an isolated-worker marker effect with
ambiguity and no redispatch, and plugin disappearance after session creation
with failure before proposal or dispatch. The older six-case plugin-node suite
continues to cover approval restart/denial, host restart, exact no-rebinding
validation, and receipt-based reconstruction without redispatch. The current
Graph C scripts additionally pass with an exact plugin executor inside a
parallel branch, carrying its validated action and artifact into the join and
proving correlated cancellation plus terminal replay without redispatch.

The dedicated Planner-worker v1.4 scripts pass on Windows with
`planner-worker v1.4 branch workspace/child tool/artifact/join/reviewer/restart
Windows E2E passed` and on Ubuntu/WSL2 with
`planner-worker v1.4 branch workspace/child tool/artifact/join/reviewer/restart
Linux E2E passed`. They prove real simultaneous child execution at two harness
gates, reverse external completion with canonical integration order, distinct
runtime-owned branch workspaces, exactly-once filesystem/process/Git tool
receipts, child-owned result/diff/test artifacts, structured revision then
approval, daemon replacement, and an unchanged parent workspace.

The dedicated TUI rich-attachment scripts pass on both platforms with
`runtime TUI rich-attachment/restart E2E passed`. They traverse the four-layer
TUI path into the versioned rich envelope, prove image/blob provider delivery,
workspace-escape rejection, transient-state clearing, daemon restart, and pure
replay; Linux additionally exercises a real symbolic link.

The native automatic-memory Ask extension passes in the existing cross-platform
`runtime_automatic_memory` process matrix. The exact captured outputs are
`.omx/validation/runtime-automatic-memory-windows.log` and
`.omx/validation/runtime-automatic-memory-linux.log`; both end with
`runtime automatic-memory crash/restart/replay/native-ask E2E passed` and cover
pending restart, approve once, duplicate no-op, and effect-free denial.

A separate arbitrary user-schedule graph process suite passed on both supported
development operating systems:

- Windows: `& .\tests\e2e\runtime_arbitrary_graph_schedule.ps1` passed with
  `runtime arbitrary schedule graph exact-plan/policy/outbox/restart/wake-once/replay E2E passed`.
- Ubuntu/WSL2: `wsl.exe bash -lc 'cd /mnt/c/Users/jkpri/AgentMod && bash
  tests/e2e/runtime_arbitrary_graph_schedule.sh'` performed the locked Linux
  build and passed with the same terminal result.
- `cargo test --locked -p agentmod-runtime-logic --all-features schedule --
  --nocapture`: 13 passed.
- `cargo clippy --locked -p agentmod-runtime-logic --all-targets --all-features
  -- -D warnings`: passed.

The fixture is a user-loaded, non-built-in graph whose persisted generation-3
plan resolves `await-wake` to exact `runtime.schedule@1.0.0`. Both process runs
bind the canonical resolution and scheduler outbox to the immutable plan and
configuration hashes, stop the runtime before polling, restart to claim the
durable one-time trigger, observe exactly one resumed continuation and terminal
scheduler marker, then restart and replay without another claim, effect, or
journal append. macOS was not run.

The independent second-harness proof was process-executed on both supported
development operating systems:

- Windows:
  `& tests\e2e\runtime_harness_selection.ps1`: passed.
- Ubuntu/WSL2, with a fresh temporary Linux `CARGO_TARGET_DIR`:
  `wsl -e bash -lc 'bash tests/e2e/runtime_harness_selection.sh'`: passed.
- `cargo test --locked -p agentmod-independent-harness-fixture --all-targets
  --all-features`: 5 passed.
- `cargo clippy --locked -p agentmod-independent-harness-fixture --all-targets
  --all-features -- -D warnings`: passed.
- `cargo tree --locked -p agentmod-independent-harness-fixture --edges normal
  --prefix none`: contains the public harness protocol and no native harness
  service, logic, data, or dependency crate.

Both process suites retain exact `independent-harness:fixture-ok` output in the
canonical journal and exact `independent-harness:fixture-restarted` output
after runtime restart. The independent process verifies the keyed grant before
provider execution and never invokes the native harness implementation.

The protocol-v9 authenticated-cancellation path was built and process-executed
on both Windows and Ubuntu/WSL2. The exact commands and results were:

- Windows:
  `cargo test -p agentmod-plugin-host --test multiplex --all-features --
  --nocapture`: 8 passed.
- Ubuntu/WSL2:
  `wsl.exe bash -lc 'cd /mnt/c/Users/jkpri/AgentMod && cargo test
  -p agentmod-plugin-host --test multiplex --all-features -- --nocapture'`:
  8 passed.
- `cargo test -p agentmod-plugin-protocol --all-features`: 17 passed; the
  full-memory-write cross-layer golden hash is
  `696facbe7de78af68a107339722d8f6871602bdc8a4fea2d5c918d4d58b6eee3`.
- `cargo test -p agentmod-runtime-dependency --all-features`: 75 passed; the
  two opt-in process helpers remained ignored.
- `cargo test -p agentmod-plugin-host-dependency --all-features --lib`:
  6 passed.
- `cargo clippy -p agentmod-plugin-protocol
  -p agentmod-runtime-dependency -p agentmod-runtime-data
  -p agentmod-plugin-host-dependency -p agentmod-plugin-host-data
  -p agentmod-plugin-host-logic -p agentmod-plugin-host-service
  -p agentmod-plugin-host --all-targets --all-features -- -D warnings`:
  passed.
- `cargo clippy -p agentmod-runtime-logic --lib --all-features --
  -D warnings`: passed.

The full runtime-managed plugin-lifecycle path was also process-executed:

- Windows:
  `& tests\e2e\runtime_plugin_lifecycle.ps1`: passed, including a TUI-originated
  disable and continuous observation of four or more externally committed
  lifecycle events.
- Ubuntu/WSL2:
  `wsl.exe bash -lc 'cd /mnt/c/Users/jkpri/AgentMod && sh
  tests/e2e/runtime_plugin_lifecycle.sh'`: passed with the same TUI management
  and live-subscription proof.
- `cargo test --locked -p agentmod-runtime-logic
  concurrent_canonical_event_reloads_head_without_redispatch`: 1 passed.
- `cargo test --locked -p agentmod-runtime-logic
  inactive_plugin_blocks_new_and_substituted_work_without_redispatch`:
  1 passed.
- `cargo test --locked -p agentmod-runtime-data
  lifecycle_change_maps_exactly_and_removes_session_activation`: 1 passed.
- `cargo test --locked -p agentmod-runtime-logic plugin_lifecycle::tests`:
  3 passed.

The production automatic plugin-memory path was also process-executed on both
Windows and Ubuntu/WSL2:

- Windows:
  `& tests\e2e\runtime_plugin_automatic_memory.ps1`: passed.
- Ubuntu/WSL2:
  `wsl -e bash -lc 'bash tests/e2e/runtime_plugin_automatic_memory.sh'`:
  passed.
- `cargo test -p agentmod-plugin-fixture-worker --test memory_hardening
  --all-features --locked -- --nocapture`: 5 passed.
- `cargo test -p agentmod-runtime-logic --lib plugin_automatic_memory --
  --nocapture`: 15 passed.
- `cargo test -p agentmod-session-style-sdk --all-targets --all-features
  --locked`: 47 passed.

These cases prove exact declaration and immutable-configuration admission,
durable Ask approval restart, post-persist receipt recovery, typed result
validation, post-worker-start timeout, missing/corrupt receipt ambiguity,
provider unavailability after session creation, and no relaunch after an
ambiguous dispatch.

The nine plugin-node process cases pass on Windows and Ubuntu/WSL2 and prove
declared runtime-action validation plus live interceptor/node/context/memory-worker
preemption, exact cancellation-receipt replay, out-of-order routing,
unknown-correlation all-waiter closure, target/configuration substitution
rejection, non-idempotent ambiguity without relaunch, and durable node-state
write/read replay. They also prove disable/quarantine cancels registered
invocations and rejects later plugin work. State CAS/read completes synchronously under the host
persistence lock, so its process test deliberately verifies exact
authorization, replay, and substitution instead of claiming a timing-race
preemption result.

The last complete repository-wide Windows validation, before the current
Research/tool-catalog/provider-batch migration edits, passed:

- `cargo fmt --all -- --check`.
- Strict workspace/all-target/all-feature Clippy with warnings denied.
- `cargo test --workspace --all-targets --all-features --locked`; runtime logic
  contributes 430 passing tests.
- `cargo test --workspace --doc --all-features --locked`.
- Both architecture commands; 89 packages have no violations and the two
  intentional-violation tests pass.
- `cargo deny check`.
- `cargo audit`.

After the Persistent v1.2 migration, the then-current focused validation passed:
runtime logic 441/441, runtime data 50/50, runtime service 15/15, and the
session-style SDK 31/31. Targeted all-target/all-feature Clippy for those four
crates passes with warnings denied, as does `cargo fmt --all -- --check`. The complete
`runtime_ephemeral_turn.ps1` process suite passes on Windows, and the matching
POSIX suite passes through actual Ubuntu/WSL2 process execution with Linux-built
runtime, harness, scheduler, and CLI binaries. Both suites include exact v1.1
selection and execution, current v1.2 generic execution, provider-receipt crash
recovery, daemon restart, and replay.

For the current shared tree, graph-engine 39/39, runtime data 61/61, runtime
dependency 74/74 on Windows and 75/75 on Linux (one additional Unix-gated
transport test), and the
session-style SDK 41/41 pass with their targeted warnings-denied Clippy checks.
Runtime logic now passes 488/488 after the Planner migration, live typed
summary, automatic-memory, and artifact-handoff slices, including
production string-task reconciliation, join-result output, shared merge replay,
exact join-cut recovery, bounded summary generation, required-record restoration,
provider projection, pure replay, immutable memory configuration, request-bound
proposal/outbox state, mandatory recovery revalidation, and idempotent dispatch
reconciliation. The full suite passes on the default Windows worker stack:
artifact-handoff crash-matrix storage is heap-bounded, and child-message
approval plus branch-denial resumes cross the existing cancellation-safe fresh
task boundary instead of recursively growing the async state machine. The
plugin-node subset adds eleven coordinator tests,
ordered reducer coverage, and a nine-case isolated Windows/Linux process run
covering exact selection, durable receipt recovery, invalid output,
unavailability, timeout, and ambiguity. Artifact tests cover exact logical/portable identity,
same-length content tampering, every phase/policy/dispatch/receipt/replacement
restart cut, and missing-object live-resume blocking. Dedicated locked Windows
and Ubuntu/WSL2 artifact-handoff process runs pass exact object/projection/
envelope binding, daemon replacement, immutable plan retention, and pure replay.
Dedicated locked Windows and Ubuntu/WSL2 automatic-memory process
runs pass the post-persist/pre-terminal daemon kill, poisoned-harness recovery,
exact lifecycle/receipt binding, single-record check, and byte-stable replay.
The dedicated `SQLite` crash-cut commands also pass against the current shared
tree:

- Windows: `& tests\e2e\runtime_sqlite_memory_crash.ps1` →
  `runtime SQLite automatic-memory windows post-commit crash/restart/replay E2E passed`.
- Ubuntu/WSL2: `wsl.exe sh -lc 'cd /mnt/c/Users/jkpri/AgentMod && sh tests/e2e/runtime_sqlite_memory_crash.sh'` →
  `runtime SQLite automatic-memory linux post-commit crash/restart/replay E2E passed`.

Both commands kill after the FTS row is committed but before
`memory.write_completed`, restart with the harness paths unavailable, and
commit exactly one terminal memory lifecycle without model redispatch. They
prove pure replay with a stable journal digest plus identical ordered semantic
rows and `PRAGMA integrity_check = ok`; no claim depends on WAL byte identity.
Locked Windows and Ubuntu/WSL2 Research process runs pass for exact frozen v1.1
and generic v1.2 execution, daemon replacement, immutable binding retention,
and pure replay. Locked Windows and Ubuntu/WSL2 Planner-worker process runs pass
for exact frozen v1.1 and generic v1.3 execution, four child sessions, two
parallel joins, reviewer revision, daemon replacement, immutable plan
retention, exact shared-read-only workspace leases, structured findings, and
pure replay. The full workspace, documentation, architecture,
supply-chain, and audit commands must be rerun after the remaining migration
work before release completion is claimed.

Earlier feature-unified regressions in Graph B review JSON ordering and
object-backed child task ordering were fixed by canonical sorted serialization
and task-ID ordering; the complete workspace command was rerun after both
fixes. Previously recorded research-loop, declarative-graph, planner-worker,
and plugin-composition process E2Es pass on Windows. Current `runtime_cli` and
`runtime_style_registry` process suites pass on both Windows and Ubuntu/WSL2
for Persistent v1.2, including exact plan retention across daemon replacement.
The declarative and Ephemeral generic paths have also been process-executed on
Linux in this phase.
`cargo audit` reports zero known vulnerabilities and one allowed unmaintained
warning for `fxhash 0.2.1`. `cargo deny check` passes with cargo-deny 0.20.2:
all 224 internal path dependencies now retain an explicit `0.1.0` compatibility
version, `xtask` declares the workspace dual license, and the
CDLA-Permissive-2.0 data license is allowed only for exact
`webpki-roots 1.0.9`. Advisories, bans, licenses, and sources are green while
the wildcard ban, unknown-registry/git denials, and duplicate-version warnings
remain enabled.

## Remaining verified limitations

- The complete mandatory workspace/all-target/all-feature, documentation,
  architecture, supply-chain, audit, and Windows/Linux release matrix has not
  been rerun against the final converged shared tree. The earlier full-validation
  result and newer focused results above remain valid only for their recorded
  revisions and scopes.
- macOS process execution has not been recorded for this phase.
- Plugin executors are proven inside one bounded parallel region; additional
  plugin-proposed runtime-action classes beyond the validated declared
  filesystem/network set and nested parallel regions are not complete.
- Planner-worker v1.4 proves wall-clock child overlap, runtime-owned branch
  workspaces, and child-owned result/diff/test evidence. Other workspace modes
  and the full write-denial matrix remain incomplete.
- ACP-declared Streamable HTTP, legacy SSE, exact branch-bootstrap copying, and
  immediate-parent child-session MCP inheritance pass real Windows/Linux
  process tests. Transitive/grandchild inheritance and macOS are not proven.
- Broader semantic DLP remains incomplete; native and plugin automatic-memory
  Ask paths are durable and cross-platform process-tested.
- TUI LSP management remains incomplete; rich attachments are cross-platform
  process-tested. Broader
  host-category crash injection and product-scale load/latency evidence are not
  established.

## Next tasks

1. Run the complete mandatory Windows and Linux validation matrix against the
   converged tree, including formatting, strict Clippy, workspace/all-target/
   all-feature and documentation tests, architecture checks, `cargo deny`, and
   `cargo audit`; record results without weakening policy.
2. Close the remaining planner workspace-mode/write-denial, additional plugin
   action and nested-parallel, broader context DLP, and TUI LSP gaps with focused
   recovery and process evidence.
3. Run the supported macOS process and validation matrix when a macOS execution
   environment is available.

## Performance results

The current recorded evidence is
`docs/benchmarks/2026-07-31-current-kernel-and-journal-stress.md`, with raw JSON
under `.omx/validation`. Both platforms used the exact recorded dirty-tree HEAD
and release-mode locked commands.

| Kernel operation | Windows ns/op | Ubuntu/WSL2 ns/op |
|---|---:|---:|
| Event envelope seal | 3,719.166 | 2,253.314 |
| Event envelope verify | 3,400.088 | 2,104.679 |
| Protocol CBOR round trip | 2,516.712 | 2,157.271 |
| Expression parse | 1,586.939 | 783.462 |
| Expression evaluate | 88.331 | 77.185 |
| Graph compile | 17,843.320 | 14,805.732 |
| Protocol decode only | 1,762.569 | 1,547.967 |

The focused journal stress run passed on both platforms: eight writers produced
400 unique checksum-linked records in one contended session, and 32 writers
produced 800 isolated records across 32 sessions. Test-reported elapsed time was
3.52 seconds on Windows and 0.87 seconds on Ubuntu/WSL2; command wall time,
including release builds, was 222.636 seconds and 167.6 seconds respectively.
These are deterministic single-process kernel measurements and buffered journal
correctness/contention observations—not runtime throughput, tail-latency,
capacity, or full-daemon stress claims. Clean converged-head refresh,
macOS, journal durability latency, replay/snapshot/artifact/context/frontend,
and cross-process benchmarks remain pending.

## Security review

Browser, filesystem, Git, LSP, MCP, process, Web, and the native harness fail closed with keyed
owner/session/call/action/digest/expiry/nonce grants and dependency-side verification.
Process and Web now reconstruct canonical action bytes from dependency-owned request
fields, bind bootstrap identity, bound concurrency, and persist replay protection across
restart. Runtime filesystem and process calls now traverse the complete proposal and
mandatory policy chain before a per-session host receives a single-use grant.
Runtime browser, Git, Web, LSP, and MCP calls traverse the same proposal and event path;
their dependencies verify exact host-specific grants. MCP additionally binds
expanded operation arguments and cancellation identifiers and consumes nonces in
durable per-session replay state before server discovery or invocation.
Pending tool actions are session-scoped and checksum protected. Continuation
resumption runs the blocking pipeline before the compare-and-set claim, and
mandatory tool policy is reapplied immediately before host dispatch. A canonical
`tool.execution_dispatched` outbox event is committed before the host boundary.
Replay resumes claims that never reached dispatch and treats terminal dispatches
as idempotent. Nonterminal approval dispatches use a receipt-only request:
checksum-protected terminal streams are bound to execution/session/call/tool,
workspace, arguments, and cancellation identity; already committed host-event
prefixes are skipped, and missing, corrupt, or conflicting receipts remain
ambiguous without redispatch. The receipt is durably written in the supervised
runtime dependency immediately after the host terminal frame and before returning
to logic. Before the RPC listener starts, the runtime scans every verified
receipt and reconciles ordinary nonterminal dispatches from canonical state.
Receipts owned by an already-approved durable continuation are classified and
left to the continuation's resume-once path so startup cannot consume the
action without also resuming its provider turn.
Scheduler startup recovery similarly separates durable occurrence claims from
canonical dispatch provenance. An unstarted claim may enter the ordinary
intercepted path; a canonically terminal claim receives a reconciliation event
before its worker marker; an ambiguous dispatched claim is terminally failed
without redispatch. Exact execution, schedule, and optional continuation
identity are retained in the canonical recovery event.
Process executable policy is explicit and deny-by-default. Its sanitized child
environment admits only non-secret platform/toolchain discovery variables plus
configured overrides; the generic secret-name filter remains mandatory. Harness grants are
ephemeral, nonce-bearing, action-digest bound, expiry
checked, and replay limited in the harness dependency layer; ambiguous failed exchanges
are never retried automatically. Process children use Windows tree termination or Unix process groups.
Runtime replacement now reattaches to the surviving authenticated process host
without redispatch; abrupt process-host crash reattachment remains intentionally
unavailable because inherited handles cannot be recreated. Web proxy upstream DNS is
necessarily delegated to an explicitly trusted proxy. Journal corruption/recovery,
mandatory permission precedence, and plugin authority validation have tests.

## Acceptance scenario status

Acceptance remains partial because of the explicit product and platform limits
above, not because execution still requires a bundled graph topology. Recorded
Windows and Ubuntu/WSL2 process matrices now cover arbitrary user Graph A
(condition/parallel/tool/approval/artifact/loop/event/delay/join), Graph B
(spawn/message/wait/join/review/revision plus accepted cancellation), Graph C
(mixed runtime/plugin execution, validated actions/state/timeout/ambiguity), and
a non-built-in schedule graph. All persist exact executor plans, survive the
tested daemon cuts, suppress duplicate effects, and replay without live
component queries.

Current planner-worker v1.4 passes on Windows and Ubuntu/WSL2 through real
overlapping child turns, runtime-owned branch workspaces, child-owned edit/test/
diff artifacts, joins, deterministic integration, evidence-bound reviewer
rejection/revision, and daemon replacement; the prior v1.3 matrix retains
pure-replay evidence and exact v1.1 remains on its frozen adapter. Other
workspace modes and the complete write-denial matrix
remain open. Current Persistent,
Ephemeral, Research, declarative, context compaction/memory, plugin context and
memory, independent-harness, MCP OAuth, ACP rich-content/session-MCP/branch,
TUI plugin lifecycle/live subscription/rich attachments, and scheduler paths have recorded
Windows and Ubuntu/WSL2 process evidence in the validation section above.

Scenario 1 still lacks symbol search and multi-file editing despite its real
read/edit/failing-test/fix/passing-test loop. Windows approval/denial, receipt
recovery, startup reconciliation, PTY reattachment, and small replay/branch
paths retain their recorded evidence; the large-history artifact branch has a
Windows/Unix pair. Tool-host scenarios 9–11 retain live boundary smoke coverage
but not every required multi-step product scenario. Scheduler time,
runtime-event, process-output, deferred-turn, TUI management, and restart
reconciliation pass on Windows and Ubuntu/WSL2; graph-node cancellation
crash-gap coverage is still narrower than the complete matrix.

Machine-readable mappings and exact limitations are in
`docs/requirements/traceability.toml`. No macOS acceptance run or final
repository-wide mandatory validation against the converged shared tree is
claimed.

## N-tier compliance status

The last complete architecture run covered 89 packages and reported no metadata
or source-layer violations; both intentional-violation fixtures emitted their
stable diagnostics. Runtime, harness, CLI, TUI, ACP, scheduler, plugin host, and
the browser/filesystem/process/Git/Web/LSP/MCP hosts retain explicit
service → logic → data → dependency layers, layer-owned types, protocol DTO
confinement, and composition-root assembly. Generic node execution, plugin
context/memory operations, scheduler observations, and ACP per-session MCP
binding were added through those existing layer boundaries rather than direct
cross-layer imports. The independent harness fixture imports only the public
harness protocol, not native harness internals. The architecture commands must
still be rerun as part of the final mandatory validation; this paragraph is not
a new full-workspace result.

## Process-boundary status

Runtime, harness, CLI, TUI, ACP, scheduler, browser, filesystem, process, Git, LSP, Web, MCP, and plugin host are
distinct binaries. Windows and Ubuntu/WSL2 process evidence now spans the native
and truly independent harnesses, arbitrary Graph A/B/C and schedule execution,
current planner-worker v1.4, scheduler time/event/process-output delivery and
recovery, plugin interceptor/node/context/memory/lifecycle paths, TUI
plugin/schedule management with continuous canonical-event subscription, MCP
OAuth, TUI rich attachments, and ACP rich-content plus immutable per-session MCP
activation, branch bootstrap copying, and immediate-parent child-session MCP
inheritance.
Those paths preserve runtime authority over canonical state and route effects
through the existing policy, continuation, receipt, scheduler, child, plugin,
and tool-host boundaries.

Windows evidence additionally covers receipt-only filesystem recovery, startup
reconciliation of unrelated nonterminal dispatches, and live PTY host
reattachment without redispatch. Incremental harness frames, per-event commits,
ordered credit-windowed RPC streams, active cancellation, flushed NDJSON, and
bounded reconnect pages remain live. Outstanding boundary evidence is scoped:
broader host-category crash injection, TUI LSP management, additional plugin
action classes and nested parallel regions, the complete planner
workspace-mode/write-denial matrix, transitive/grandchild MCP inheritance, and
macOS.
The final mandatory cross-platform
validation against the converged tree has not yet been run.
