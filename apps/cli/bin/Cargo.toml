[package]
name = "agentmod-cli"
description = "Headless command-line frontend for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "agentmod"
path = "src/main.rs"

[dependencies]
agentmod-cli-data = { path = "../data" }
agentmod-cli-dependency = { path = "../dependency" }
agentmod-cli-logic = { path = "../logic" }
agentmod-cli-service = { path = "../service" }
agentmod-protocol-support = { path = "../../../core/protocol-support" }

[lints]
workspace = true

[package.metadata.agentmod]
kind = "composition-root"
process = "cli"
