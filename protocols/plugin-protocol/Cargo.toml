[package]
name = "agentmod-plugin-protocol"
description = "Versioned isolated plugin-host wire contracts for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
agentmod-primitives = { version = "0.1.0", path = "../../core/primitives" }
semver = "1.0"
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
agentmod-plugin-sdk = { version = "0.1.0", path = "../../sdk/plugin-sdk" }

[lints]
workspace = true

[package.metadata.agentmod]
kind = "protocol"
family = "plugin"
