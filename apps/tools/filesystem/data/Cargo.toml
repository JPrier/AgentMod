[package]
name = "agentmod-filesystem-host-data"
description = "Business-facing filesystem datasets for the AgentMod filesystem host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
agentmod-filesystem-host-dependency = { version = "0.1.0", path = "../dependency" }
thiserror.workspace = true

[lints]
workspace = true

[package.metadata.agentmod]
kind = "process-layer"
process = "filesystem"
layer = "data"
