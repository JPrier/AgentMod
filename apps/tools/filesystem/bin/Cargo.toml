[package]
name = "agentmod-filesystem-host"
description = "Isolated native filesystem capability host for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "agentmod-filesystem-host"
path = "src/main.rs"

[dependencies]
agentmod-filesystem-host-data = { version = "0.1.0", path = "../data" }
agentmod-filesystem-host-dependency = { version = "0.1.0", path = "../dependency" }
agentmod-filesystem-host-logic = { version = "0.1.0", path = "../logic" }
agentmod-filesystem-host-service = { version = "0.1.0", path = "../service" }
agentmod-protocol-support = { version = "0.1.0", path = "../../../../core/protocol-support" }
agentmod-tool-protocol = { version = "0.1.0", path = "../../../../protocols/tool-protocol" }
serde_json.workspace = true

[dev-dependencies]
agentmod-primitives = { version = "0.1.0", path = "../../../../core/primitives" }
tempfile.workspace = true

[lints]
workspace = true

[package.metadata.agentmod]
kind = "composition-root"
process = "filesystem"
