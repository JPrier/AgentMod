[package]
name = "agentmod-scheduler-protocol"
description = "Versioned runtime-to-scheduler worker wire contracts for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true

[lints]
workspace = true

[package.metadata.agentmod]
kind = "protocol"
family = "scheduler"
