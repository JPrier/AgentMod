[package]
name = "agentmod-harness-fixture"
description = "Independent deterministic harness fixture process for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
agentmod-harness-fixture-data = { path = "../data" }
agentmod-harness-fixture-dependency = { path = "../dependency" }
agentmod-harness-fixture-logic = { path = "../logic" }
agentmod-harness-fixture-service = { path = "../service" }
agentmod-harness-protocol = { path = "../../../protocols/harness-protocol" }
serde_json.workspace = true
tokio = { workspace = true, features = ["io-std"] }

[lints]
workspace = true

[package.metadata.agentmod]
kind = "composition-root"
process = "harness-fixture"

[dev-dependencies]
agentmod-primitives = { path = "../../../core/primitives" }
blake3.workspace = true
uuid.workspace = true
