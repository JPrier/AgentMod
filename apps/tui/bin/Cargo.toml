[package]
name = "agentmod-tui"
description = "First-class terminal frontend for AgentMod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "agentmod-tui"
path = "src/main.rs"

[dependencies]
agentmod-protocol-support = { version = "0.1.0", path = "../../../core/protocol-support" }
agentmod-tui-data = { version = "0.1.0", path = "../data" }
agentmod-tui-dependency = { version = "0.1.0", path = "../dependency" }
agentmod-tui-logic = { version = "0.1.0", path = "../logic" }
agentmod-tui-service = { version = "0.1.0", path = "../service" }

[lints]
workspace = true

[package.metadata.agentmod]
kind = "composition-root"
process = "tui"
