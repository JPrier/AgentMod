[package]
name = "fixture-runtime-dependency"
version = "0.0.0"
edition = "2021"

[package.metadata.agentmod]
kind = "process-layer"
process = "runtime"
layer = "dependency"

[dependencies]
fixture-runtime-logic = { version = "0.1.0", path = "../logic" }
